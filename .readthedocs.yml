version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python:
      version: "3.8"

python:
  version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

formats: all
